import 'package:flutter/material.dart';

/// Selector de tipo de combustible
class FuelSelector extends StatelessWidget {
  const FuelSelector({super.key});
  
  @override
  Widget build(BuildContext context) {
    // TODO: Implement - Selector de combustible
    // - Botones para Gasolina 95 y Diésel Gasóleo A
    // - Estilo seleccionado/no seleccionado
    // - onTap para cambiar selección
    // - Emitir evento ChangeFuelType al MapBloc
    return const Row(
      children: [
        Text('TODO: FuelSelector'),
      ],
    );
  }
}
